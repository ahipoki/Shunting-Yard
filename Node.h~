#ifndef NODE_H
#define NODE_H
#include <iostream>

using namespace std;

class Node
{
 public:
  Node(char*);
  ~Node();
  setLeft();
  getLeft();
  setRight();
  getRight();
  setNext();
  getNext();
 private:
  Node* next;
};

#endif
